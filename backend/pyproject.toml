[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "sakuraplayer"
version = "0.1.0"
description = "Private SakuraPlayer backend"
requires-python = "==3.10.16"
license = { text = "GPL-3.0-only" }
dependencies = [
  "alembic==1.16.2",
  "APScheduler==3.10.4",
  "argon2-cffi==23.1.0",
  "cryptography==45.0.4",
  "defusedxml==0.7.1",
  "fastapi==0.110.1",
  "httpx==0.28.1",
  "Pillow==11.2.1",
  "psycopg[binary]==3.2.9",
  "pydantic==2.7.4",
  "pydantic-settings==2.2.1",
  "PyJWT==2.10.1",
  "SQLAlchemy==2.0.41",
  "starlette==0.37.2",
  "uvicorn==0.22.0",
]

[project.optional-dependencies]
test = [
  "mypy==2.3.0",
  "pytest==8.4.1",
  "pytest-asyncio==1.0.0",
  "ruff==0.16.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/sakuraplayer"]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
pythonpath = ["src"]
testpaths = ["tests"]
norecursedirs = ["tests/real115"]
markers = [
  "host_docker: requires the host Docker CLI",
  "integration: requires the isolated PostgreSQL test container",
  "real115: requires explicit real 115 credentials and an application-managed test root",
]

[tool.ruff]
line-length = 88
target-version = "py310"
extend-exclude = ["alembic/versions"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
ignore = ["E731"]
